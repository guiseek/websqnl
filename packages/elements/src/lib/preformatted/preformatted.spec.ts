import {Preformatted} from './preformatted'

describe('preformatted', () => {
  it('should create instance', () => {
    const element = new Preformatted()

    expect(element).toBeInstanceOf(Preformatted)
  })
})
