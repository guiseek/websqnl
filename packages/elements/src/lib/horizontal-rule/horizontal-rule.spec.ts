import {HorizontalRule} from './horizontal-rule'

describe('horizontal-rule', () => {
  it('should create instance', () => {
    const element = new HorizontalRule()

    expect(element).toBeInstanceOf(HorizontalRule)
  })
})
