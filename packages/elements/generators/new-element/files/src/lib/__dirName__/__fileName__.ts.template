import {assign, builtIn} from "../../shared"

@builtIn('<%= is %>', 'web-<%= fileName %>')
export class <%= className %> extends <%= htmlClassName %> {
  constructor(attrs: Partial<<%= className %>> = {}) {
    super()
    assign(this, attrs)
  }
}